git init  
git add .  
git commit -m "segundo commit"  
git remote add origin git@github.com:HenriqueeK/Desenvolvimento_web.git  
git push -u origin main  
Só para comentar eu já tinha o git baixado e configurado com meu nome e email,
então não monstrei esses passos..  
# Desenvolvimento_web
